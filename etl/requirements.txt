# ==============================================================================
# Projeto BD Polifarmácia — Dependências Python
# ==============================================================================
# Instalar com: pip install -r requirements.txt
# Recomenda-se usar um ambiente virtual (venv/conda).
# ==============================================================================

# --- Banco relacional (PostgreSQL) ---
psycopg2-binary==2.9.9        # Driver PostgreSQL (binário, sem compilar libpq)

# --- Banco de grafos (Neo4j) ---
neo4j==5.26.0                  # Driver oficial Neo4j para Python

# --- Manipulação de dados ---
pandas==2.2.3                  # DataFrames para leitura e transformação dos CSVs
numpy==2.1.3                   # Dependência do pandas, fixada para reprodutibilidade

# --- Utilitários ---
python-dotenv==1.0.1           # Carregar variáveis de ambiente do .env
tqdm==4.67.1                   # Barras de progresso para operações longas
