# ==============================================================================
# Projeto BD Polifarmácia — Dependências da app Streamlit
# Instalar com: pip install -r app/requirements.txt
# ==============================================================================

streamlit==1.40.2          # framework de UI
psycopg2-binary==2.9.9     # driver PostgreSQL (GCP Cloud SQL)
neo4j==5.26.0              # driver Neo4j (GCP)
pandas==2.2.3             # DataFrames
python-dotenv==1.0.1      # leitura do .env
pyvis==0.3.2             # grafos interativos (HTML/vis.js)
